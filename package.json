{
  "name": "bamazon",
  "version": "1.0.0",
  "description": "below will identify all the node/npm modules that are referenced in the folder yields the required list for \"npm install console.table dotenv inquirer mysql\" which has to be run form command line to populate node_modules folder",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "console.table": "^0.10.0",
    "inquirer": "^6.2.1",
    "mysql": "^2.16.0",
    "mysql2": "^1.6.4",
    "sequelize": "^4.42.0"
  },
  "devDependencies": {}
}
